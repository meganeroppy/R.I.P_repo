using UnityEngine;
using System.Collections;

public class SpiritBarFrame_canvas : MonoBehaviour
{
	private Vector3 pos = new Vector3 (10.0f, 250.0f, -20.0f);
	private Vector2 size = new Vector2 (0.0f, 0.0f);
	
	//Player 
	
	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
//		RectTransform rect = this.GetComponent<RectTransform>();
		
//		rect.position = pos;
//		rect.sizeDelta = size;
//		rect.pivot = new Vector2(0.0f, 0.5f);
		
	}
}